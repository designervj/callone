"use client"

import { useSelector } from "react-redux";
import { RootState } from "@/store";

const BrandTable = () => {
  
    return (
        <div>
            <h1>Brand Table</h1>
        </div>
    );
}

export default BrandTable;